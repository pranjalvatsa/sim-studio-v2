{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd apps/sim && bun install --frozen-lockfile && DATABASE_URL=postgresql://dummy:dummy@localhost:5432/dummy BETTER_AUTH_SECRET=dummy NODE_ENV=production bun run build"
  },
  "deploy": {
    "startCommand": "cd apps/sim && NODE_ENV=production next start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 16
  }
}
